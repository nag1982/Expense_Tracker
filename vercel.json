{
  "version": 2,
  "env": {
    "DATABASE_PATH": "/tmp/expense_tracker.db"
  },
  "builds": [{
    "src": "app.py",
    "use": "@vercel/python",
    "config": {
      "includeFiles": ["templates/**", "static/**"]
    }
  }],
  "routes": [{ "src": "/(.*)", "dest": "app.py" }]
}
